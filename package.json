{
  "name": "@syfxlin/depker",
  "scripts": {
    "build:cli": "pnpm --filter @syfxlin/depker-cli build",
    "build:client": "pnpm --filter @syfxlin/depker-client build",
    "build:server": "pnpm --filter @syfxlin/depker-server build"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
